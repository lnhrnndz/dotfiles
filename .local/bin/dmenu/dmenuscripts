#!/bin/sh

scripts=$(find "$HOME"/bin/dmenu/* | grep -v -F "dmenuscripts")
options=$(echo "$scripts" | sed "s|${HOME}/bin/dmenu/||")

n="$(echo "$options" | wc -w)"

script=$(echo "$options" | dmenu -m 0 -l "$n" -bw 4 -p "select script ")

[ -z "$script" ] && exit 

exec "$script"
