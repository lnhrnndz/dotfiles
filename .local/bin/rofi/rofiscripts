#!/bin/sh

scripts=$(find "$HOME"/.local/bin/rofi/* | grep -v -F "rofiscripts")
options=$(echo "$scripts" | sed "s|${HOME}/.local/bin/rofi/||")

n="$(echo "$options" | wc -w)"

script=$(echo "$options" | rofi -dmenu -l "$n" -p "select script ")

[ -z "$script" ] && exit 1

exec "$script"
