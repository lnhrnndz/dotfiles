#!/bin/sh

scripts=$(find "$HOME"/bin/dmenu/* | grep -v -F "dmenuscripts")
options=$(echo "$scripts" | sed "s|${HOME}/bin/dmenu/||")

n="$(echo "$options" | wc -w)"

script=$(echo "$options" | dmenu -m 0 -c -i -l "$n" -p "Select script")

[ -z "$script" ] && exit 

exec "$script"
